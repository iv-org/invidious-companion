version: 2
updates:
  # Docker and GitHub Actions updates now handled by Renovate
  # Keeping this configuration as backup but disabled
  # - package-ecosystem: "docker"
  #   directory: "/"
  #   schedule:
  #     interval: "weekly"
  # - package-ecosystem: github-actions
  #   directory: /
  #   schedule:
  #     interval: "weekly"
  
  # Dependabot doesn't support Deno dependencies, so we use Renovate instead
  # See renovate.json for comprehensive dependency management
